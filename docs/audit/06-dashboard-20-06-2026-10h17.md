# Audit — Page « Dashboard »

**Fichier :** `pages/index.vue` + `components/AnalyticsDashboard.vue`
**Route :** `/`

## État actuel

| Élément | Statut | Détail |
|---------|--------|--------|
| Badge "Mode dégradé" | ✅ **Supprimé** | Retiré lors du nettoyage SSR-only |
| Carte Identité (KPI) | ❌ **Simulé** | Vient de `identityResolver.getCurrentIdentity()` (en RAM) |
| Carte Flows (KPI) | ✅ **Réel** | Vient de `contextAnalyzer` (analyse FS réelle) |
| Carte Outils (KPI) | ✅ **Réel** | Vient de `contextAnalyzer` (détection réelle) |
| Carte Agents (KPI) | ❌ **Simulé** | `['ollama', 'lm-studio', 'codestral', 'generic']` en dur |
| Section Identité courante | ❌ **Simulé** | Mêmes données que la carte : identité en RAM |
| Section État du projet | ✅ **Réel** | Phase, framework, langage depuis analyse FS |
| Section Flows dans État projet | ✅ **Réel** | Depuis analyse FS (`.kiro/specs/`) |
| SSR `?folder=` support | ✅ **Fonctionnel** | `setWorkFolder(folderParam)` en SSR |
| Liens rapides | ✅ **Fonctionnel** | NuxtLinks vers prompts, templates, identités |

## Problèmes

- **Carte "Agents"** : tableau `['ollama', 'lm-studio', 'codestral', 'generic']` en dur, pas de vraie détection
- **Carte "Identité"** : dépend d'`IdentityResolver` qui est en RAM seulement
- **Section Identité courante** : double emploi avec la carte KPI Identité du AnalyticsDashboard
- **Données pas à jour sans refresh** : les données sont figées au moment du `onMounted`

## Notes pour dev-plan

- La carte Agents devra être connectée à un vrai service LLM plus tard (l'utilisateur veut la garder)
- Simplifier : éviter le double affichage Identité (KPI + section)
- Ajouter un indicateur de fraîcheur des données (date du dernier refresh)
